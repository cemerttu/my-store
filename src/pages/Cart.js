import React from "react";
const Cart = () => (
  <div className="container my-5">
    <h1>Shopping Cart</h1>
    <p>Your cart is empty (static demo). For functionality we can add React Context next.</p>
  </div>
);
export default Cart;
